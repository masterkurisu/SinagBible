/** Flip to true in dev to restart journal/reader new-entry editor onboarding on every form open. */
export const JOURNAL_EDITOR_ONBOARDING_DEBUG_ALWAYS_START = __DEV__ && true;

/** Tune coachmark offsets in `journalEditorOnboardingSteps.ts` (`journalEditorCoachmarkVerticalOffsetPx`). */
