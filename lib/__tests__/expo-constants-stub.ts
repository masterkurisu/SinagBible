const Constants = {
  expoConfig: { extra: {} as { yvpAppKey?: string } },
  manifest2: null as { extra?: { yvpAppKey?: string } } | null,
  manifest: null as { extra?: { yvpAppKey?: string } } | null,
};

export default Constants;
